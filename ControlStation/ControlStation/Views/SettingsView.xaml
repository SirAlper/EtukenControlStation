<UserControl x:Class="ControlStation.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <UserControl.Resources>
        <Style x:Key="TabButtonStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="#1E1E24"/>
            <Setter Property="Foreground" Value="#777777"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Width" Value="180"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Margin="5" BorderBrush="#333" BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="#007ACC"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="BorderThickness" Value="0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#121212">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="450">

            <TextBlock Text="İHA HABERLEŞME SİSTEMİ" Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
            <TextBlock Text="Kullanılacak haberleşme protokolünü seçin" Foreground="#AAAAAA" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,30"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                <RadioButton Content="🌐 TCP AĞ" IsChecked="{Binding IsTcpTabActive}" Style="{StaticResource TabButtonStyle}"/>
                <RadioButton Content="📡 RF TELEMETRİ" IsChecked="{Binding IsRfTabActive}" Style="{StaticResource TabButtonStyle}"/>
            </StackPanel>

            <Border Background="#1E1E24" CornerRadius="12" BorderBrush="#333333" BorderThickness="1" Padding="30">
                <Grid>

                    <StackPanel Visibility="{Binding IsTcpTabActive, Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="IP ADRESİ" Foreground="#777777" FontSize="12" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding TargetIpAddress}" Height="40" Background="#2A2A35" Foreground="White" FontSize="16" BorderThickness="0" Padding="10,8,0,0" Margin="0,0,0,20" />

                        <TextBlock Text="PORT BİLGİSİ" Foreground="#777777" FontSize="12" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding TargetPort}" Height="40" Background="#2A2A35" Foreground="White" FontSize="16" BorderThickness="0" Padding="10,8,0,0" Margin="0,0,0,30" />

                        <Button Command="{Binding ConnectTcpCommand}" Height="50" Background="#007ACC" Foreground="White" FontSize="16" FontWeight="Bold" BorderThickness="0" Margin="0,0,0,10" Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Content" Value="TCP İLE BAĞLAN"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>

                    <StackPanel Visibility="{Binding IsRfTabActive, Converter={StaticResource BoolToVis}}">
                        <Grid Margin="0,0,0,5">
                            <TextBlock Text="COM PORT SEÇİMİ" Foreground="#777777" FontSize="12" FontWeight="Bold" VerticalAlignment="Center"/>
                            <Button Content="🔄 Yenile" Command="{Binding RefreshPortsCommand}" Background="Transparent" Foreground="#00E5FF" BorderThickness="0" HorizontalAlignment="Right" Cursor="Hand"/>
                        </Grid>

                        <ComboBox IsEditable="True" Text="{Binding SelectedPort, UpdateSourceTrigger=PropertyChanged}" 
          ItemsSource="{Binding AvailablePorts}" Height="40" Background="#2A2A35" Foreground="#2A2A35" 
          FontSize="16" BorderThickness="0" Margin="0,0,0,20" Padding="10,0,0,0"/>

                        <TextBlock Text="BAUD RATE" Foreground="#777777" FontSize="12" FontWeight="Bold" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding RfBaudRate}" Height="40" Background="#2A2A35" Foreground="White" FontSize="16" BorderThickness="0" Padding="10,8,0,0" Margin="0,0,0,30" />

                        <Button Command="{Binding ConnectRfCommand}" Height="50" Background="#00E5FF" Foreground="Black" FontSize="16" FontWeight="Bold" BorderThickness="0" Margin="0,0,0,10" Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Content" Value="RF İLE BAĞLAN"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>

                    <Button Command="{Binding DisconnectCommand}" Height="50" Background="#DC3545" Foreground="White" FontSize="16" FontWeight="Bold" BorderThickness="0" Margin="0,0,0,10" Cursor="Hand" VerticalAlignment="Bottom">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Content" Value="BAĞLANTIYI KES"/>
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                </Grid>
            </Border>

            <Border Background="#1E1E24" CornerRadius="8" Margin="0,20,0,0" Padding="15">
                <TextBlock Text="{Binding StatusMessage}" Foreground="#FFD700" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center" TextWrapping="Wrap"/>
            </Border>

        </StackPanel>
    </Grid>
</UserControl>