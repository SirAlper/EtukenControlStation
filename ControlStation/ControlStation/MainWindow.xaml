<Window x:Class="ControlStation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ControlStation"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="ModernNavButton" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#888888"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="60"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" CornerRadius="0,8,8,0" Margin="0,5,15,5">
                            <Grid>
                                <Border x:Name="ActiveIndicator" Width="4" HorizontalAlignment="Left" Background="#00E5FF" Visibility="Hidden" CornerRadius="0,4,4,0"/>
                                <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Left" Margin="30,0,0,0"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1A1A20"/>
                                <Setter Property="Foreground" Value="#E0E0E0"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Background" Value="#12232E"/>
                                <Setter Property="Foreground" Value="#00E5FF"/>
                                <Setter TargetName="ActiveIndicator" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#121215" BorderBrush="#1F1F24" BorderThickness="0,0,1,0">
            <StackPanel Margin="0,30,0,0">

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,40">
                    <TextBlock Text="🚀" FontSize="24" Margin="0,0,10,0"/>
                    <TextBlock Text="GCS" Foreground="White" FontSize="22" FontWeight="Black" VerticalAlignment="Center"/>
                    <TextBlock Text="26" Foreground="#00E5FF" FontSize="22" FontWeight="Black" VerticalAlignment="Center"/>
                </StackPanel>

                <RadioButton Content="Uçuş Ekranı" Style="{StaticResource ModernNavButton}" 
                             Command="{Binding NavigateCommand}" CommandParameter="Dashboard" IsChecked="True"/>

                <RadioButton Content="Bağlantı Ayarları" Style="{StaticResource ModernNavButton}" 
                             Command="{Binding NavigateCommand}" CommandParameter="Settings"/>

                
            </StackPanel>
        </Border>
        <Border Grid.Column="1" Background="#121215" BorderBrush="#1F1F24" BorderThickness="0,0,1,0">
            <ContentControl Grid.Column="1" Content="{Binding CurrentView}" Margin="20"/>
        </Border>
                
    </Grid>
</Window>
